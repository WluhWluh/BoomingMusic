name: Build Release APK

on:
  push:
    tags: [ 'v*' ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'zulu'
          cache: gradle

      - name: Setup Android-SDK
        uses: android-actions/setup-android@v4

      - name: Create files for signing
        env:
          SIGNING_KEY: ${{ secrets.SIGNING_KEY }}
          SIGNING_PROPERTIES: ${{ secrets.SIGNING_PROPERTIES }}
        run: |
          if [ -n "$SIGNING_KEY" ] && [ -n "$SIGNING_PROPERTIES" ]; then
            printf '%s' "$SIGNING_KEY" | base64 -d > ./booming_keystore.jks
            printf '%s' "$SIGNING_PROPERTIES" | base64 -d > ./keystore.properties
          else
            echo "Signing secrets are not configured; release builds will use the debug signing fallback."
          fi

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      - name: Build app
        env:
          LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
          LASTFM_SECRET: ${{ secrets.LASTFM_SECRET }}
        run: |
          ./gradlew assembleGithubRelease

      - name: Verify downloadable LiteRT supply chain
        run: |
          python -m unittest discover -s tools -p "test_verify_litert_native.py"
          python tools/verify_litert_native.py
          python tools/verify_litert_apks.py "app/build/outputs/apk/github/release"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: artifacts-github-release
          path: app/build/outputs/apk/github/release/BoomingSS-*.apk

  publish-release:
    needs: [build]
    runs-on: ubuntu-latest
    if: > # TEMPORARY: Prevent releases for alpha/beta/rc tags.
      !contains(github.ref_name, 'alpha') && 
      !contains(github.ref_name, 'beta') && 
      !contains(github.ref_name, 'rc')

    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-artifacts
          pattern: artifacts-*-release
          merge-multiple: true

      - name: Set prerelease flag
        id: prerelease
        run: |
          TAG="${{ github.ref_name }}"
          if [[ "$TAG" == *alpha* || "$TAG" == *beta* || "$TAG" == *rc* ]]; then
            echo "value=true" >> $GITHUB_OUTPUT
          else
            echo "value=false" >> $GITHUB_OUTPUT
          fi

      - name: Github Release
        uses: softprops/action-gh-release@v3
        with:
          prerelease: ${{ steps.prerelease.outputs.value }}
          generate_release_notes: true
          body: |
            32-bit x86 and high-memory models may fail depending on device memory layout.
          files: |
            release-artifacts/BoomingSS-*.apk
