package com.mardous.booming.debug

import android.app.Activity

/** Keeps the debug main process foreground without creating player UI callbacks. */
class SourceSeparationProcessValidationActivity : Activity()
